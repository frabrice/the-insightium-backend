# Server Configuration
PORT=5000

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/theinsight

# Environment
NODE_ENV=development

# CORS Configuration (optional)
# CORS_ORIGIN=* (allows all origins - default)
# CORS_ORIGIN=http://localhost:3000 (specific origin)
# CORS_ORIGIN=http://localhost:3000,https://yourdomain.com (multiple origins)

# JWT Configuration (add these for authentication)
# JWT_SECRET=your_jwt_secret_here
# JWT_EXPIRE=8h